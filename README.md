# House-Price-Predictor
A house price prediction model built from scratch using NumPy. Includes custom Gradient Descent regression and KNN, with full preprocessing, normalization, train/validation/test pipeline, metric evaluation, and visualizations. Designed to demonstrate core ML logic without scikit-learn.
